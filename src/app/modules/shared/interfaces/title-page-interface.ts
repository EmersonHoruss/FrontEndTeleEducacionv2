export interface TitlePageInterface {
    // as name says
    titlePage: string;
    // as name says
    actionPage?: string;
    // as name says
    sectionPage?: string;
    // as name says
    ngStyle?: any;
  }
  