import { ColumTableProgramationsPipe } from './colum-table-programations.pipe';

describe('ColumTableProgramationsPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumTableProgramationsPipe();
    expect(pipe).toBeTruthy();
  });
});
